# docs

Project documentation lives here — architecture decisions, sprint plans, roadmaps.

The `/principal` skill writes its greenfield discovery output to `docs/sprints/`
(`00-overview.md` plus one file per sprint), and `/triage` weighs feature requests against
`docs/roadmap.md` when one exists. Neither is required to start.
